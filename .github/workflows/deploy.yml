# NOTE: This workflow is DISABLED since we moved to Netlify
# Netlify handles deployments automatically via Git integration
# Keeping this file for reference only

name: Deploy to GitHub Pages (DISABLED)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: echo "This workflow is disabled. Netlify handles deployments now."
